# Site Build Settings

# Jekyll-paginate-v2 configuration
#  pagination:
#   enabled: true
#   per_page: 5
#   permalink: '/page/:num/'
#   title: ':title - page :num of :max'
#   limit: 0
#   sort_field: 'date'
#   sort_reverse: true

# jekyll-paginate configuration
paginate:            5

# Build settings
markdown:            kramdown
highlighter:         rouge
permalink:           /:title
plugins:             [jekyll-paginate, jekyll-sitemap, jekyll-feed, jekyll-seo-tag, jekyll-jupyter-notebook]

# Customise atom feed settings (this is where Jekyll-Feed gets configuration information)
title:		           'Dynamic  Sparsity  in  Machine  Learning'
subtitle:		         'Routing Information through Neural Pathways'
description:	       'NeurIPS 2024 Tutorial'
author:              'Edoardo Ponti and Andre Martins'

# RSS 2.0 can be used instead of Atom by uncommenting following two lines
#feed:
#  path: rss-feed.xml

hide_post_date: true
hide_post_share: true
hide_related_posts: true

collections:
  authors:
  panellists:

